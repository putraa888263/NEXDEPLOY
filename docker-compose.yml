services:
  panel:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      NEXDEPLOY_ENCRYPTION_KEY: ${NEXDEPLOY_ENCRYPTION_KEY}
      NEXA_SERVICE_SECRET: ${NEXA_SERVICE_SECRET}
      WRANGLER_WRITE_LOGS: "false"
    ports:
      - "10.90.100.3:${PANEL_PORT:-8088}:3000"
    volumes:
      - panel-state:/app/.wrangler
    depends_on:
      - executor
    networks:
      - nexdeploy-public
      - nexdeploy-internal

  executor:
    build:
      context: ./executor
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 8787
      EXECUTOR_TOKEN: ${EXECUTOR_TOKEN}
      PROJECTS_DIR: /opt/nexdeploy/projects
    volumes:
      - executor-projects:/opt/nexdeploy/projects
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - nexdeploy-internal

volumes:
  panel-state:
  executor-projects:

networks:
  nexdeploy-public:
    driver: bridge

  nexdeploy-internal:
    internal: true
