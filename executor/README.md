# NEXDEPLOY Executor

Service lokal untuk menerima job deployment dari panel. Jalankan dengan `npm run executor:dev` dari folder root, lalu periksa `http://127.0.0.1:8787/health`.

Salin `.env.example` menjadi `.env` sebelum dipakai di VPS dan ganti token bawaan.
